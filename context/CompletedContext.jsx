import { createContext, useEffect } from "react";
import {completeMissions} from "../api/contentApi";
const MissionContext = createContext();

export default function MissionCompleteProvider(){
    useEffect(()=>{
        
    },[])
}